# Implementation Plan — SearchClinicDoctor

> Generated by `/service_unit_testing_4_steps` on 2026-07-26.
> Target service: `ECS.Application.Services.ClinicDoctorDiscoveryService.SearchClinicDoctorServices.SearchClinicDoctorService`
> Target path: `BE/api/ECSBackendAPI/ECSBackendAPI/ECS.Application/Services/ClinicDoctorDiscoveryService/SearchClinicDoctorServices/SearchClinicDoctorService.cs`
> Test target path: `BE/api/ECSBackendAPI/ECSBackendAPI/ECS.Test/Services/ClinicDoctorDiscoveryServices/SearchClinicDoctorServices/SearchClinicDoctorServiceTests.cs`
> Goal: **100% line coverage** on `SearchClinicDoctorService.cs`.

---

## Step 1 — Read & Analyze (Done)

### Surface
- **Public contract**: `ISearchClinicDoctorService.Process(SearchClinicDoctorRequest request)` → `Task<ApiResponse<SearchClinicDoctorResponse>>`.
- **Dependencies (2)**:
  1. `IRepositoryQueryBase<Clinic, Guid, AppDbContext>` — queried by `SearchClinicsAsync`.
  2. `IRepositoryQueryBase<DoctorProfile, Guid, AppDbContext>` — queried by `SearchDoctorsAsync`.

### Source lines (`SearchClinicDoctorService.cs`)
```
L39-49   Process(request) : public entry point — 4 sequential steps
L54-60   NormalizeKeyword(keyword) : private static — null/empty/whitespace → "", else Trim().ToLower()
L65-88   SearchClinicsAsync(keyword) : private async — FindByCondition + OrderBy + Select + ToListAsync
L93-122  SearchDoctorsAsync(keyword) : private async — FindByCondition + 3x Include + Where + OrderBy + Select + ToListAsync
L127-136 BuildResponse(clinics, doctors) : private static — plain object construction
L141-148 CreateSuccessResponse(response) : private static — wraps in ApiResponse<T>.Success
```

### Execution paths discovered
| # | Path | How triggered |
|---|------|---------------|
| P1 | Keyword = null → `NormalizeKeyword` returns `""` → clinic/doctor query take the "no-keyword" branch | `request.Keyword = null` |
| P2 | Keyword = `""` → `NormalizeKeyword` returns `""` (empty branch) | `request.Keyword = ""` |
| P3 | Keyword = `"   "` → `NormalizeKeyword` returns `""` (whitespace branch) | `request.Keyword = "   "` |
| P4 | Keyword = `"  SAIGON EYE  "` → `NormalizeKeyword` returns `"saigon eye"` (Trim+ToLower branch) | `request.Keyword = "  SAIGON EYE  "` |
| P5 | Clinic search with matching name → 1 row returned | seed clinic named "Saigon Eye" |
| P6 | Clinic search empty result → empty list returned | seed list empty |
| P7 | Doctor search with matching name → 1 row returned | seed doctor with User.FullName = "Nguyen Van A" |
| P8 | Doctor search with null Specialty → `Specialty.Name` branch falls through to null | seed Specialty = null |
| P9 | Doctor search with null `ReviewCount` → falls back to `0` | seed `ReviewCount = null` |
| P10 | Clinic search with null `ReviewCount` → falls back to `0` | seed `ReviewCount = null` |
| P11 | End-to-end happy path → both clinics & doctors populated, response `APP_MESSAGE_2000` | combined seed |
| P12 | Empty clinics, empty doctors → success with empty lists | empty seed |

### Empty-branch hunt
- ✅ `NormalizeKeyword` empty branch — covered by P1, P2, P3.
- ✅ `NormalizeKeyword` non-empty branch — covered by P4.
- ✅ `SearchClinicsAsync` keyword empty branch (`keyword == string.Empty`) — covered by P1/P2/P3 (any test where keyword is empty).
- ✅ `SearchClinicsAsync` keyword non-empty branch (`Contains`) — covered by P4 + P5.
- ✅ `SearchDoctorsAsync` keyword empty branch — covered by P1/P2/P3.
- ✅ `SearchDoctorsAsync` keyword non-empty branch — covered by P4 + P7.
- ✅ `SearchDoctorsAsync` `Specialty != null` true branch — covered by P7.
- ✅ `SearchDoctorsAsync` `Specialty != null` false branch (→ null) — covered by P8.
- ✅ `??` null-coalesce on `ReviewCount` for both clinics & doctors — covered by P9 + P10.

### Refactor suggested for testability?
- **None.** The service is read-only, returns `ApiResponse<SearchClinicDoctorResponse>`, and has no side-effects. All dependencies are interfaces. `MockQueryable.Moq` provides `BuildMockDbSet<Clinic>()` and `BuildMockDbSet<DoctorProfile>()` which honor `Where`, `OrderBy`, `Select`, `ToListAsync`; `.Include()` calls are no-ops but the projection still works because the seed rows are pre-populated with navigation property instances.

---

## Step 2 — Testability Audit (Done)

- **Return type**: ✅ `Task<ApiResponse<SearchClinicDoctorResponse>>` — fully inspectable.
- **Async chain**: ✅ all awaits consume simple mocks.
- **Mocking surface**: ✅ all 2 dependencies are interfaces.

Conclusion: **no refactor required**.

---

## Step 3 — Implementation Plan (THIS DOCUMENT → awaiting user approval)

### Required changes
None.

### Test cases (`[Method]_[State]_[Outcome]`)

**Process(...) tests** (covers `Process`, `NormalizeKeyword`, `SearchClinicsAsync`, `SearchDoctorsAsync`, `BuildResponse`, `CreateSuccessResponse`)

| TC | Name | Path | Assertion highlights |
|----|------|------|----------------------|
| TC-SCD-01 | `Process_NullKeyword_ReturnsAllActiveClinicsAndDoctors` | P1 | Keyword null → both queries receive `""` → all seeded clinics/doctors returned. Covers `NormalizeKeyword` null branch + both empty-keyword branches |
| TC-SCD-02 | `Process_EmptyKeyword_ReturnsAllActiveClinicsAndDoctors` | P2 | Keyword `""` → same behaviour as TC-01, exercises `NormalizeKeyword` empty-branch |
| TC-SCD-03 | `Process_WhitespaceKeyword_ReturnsAllActiveClinicsAndDoctors` | P3 | Keyword `"   "` → exercises `NormalizeKeyword` whitespace branch (`IsNullOrWhiteSpace`) |
| TC-SCD-04 | `Process_KeywordTrimsAndLowercases` | P4 | Keyword `"  SAIGON EYE  "` → asserts clinic whose Name.ToLower().Contains("saigon eye") is returned; clinic whose Name.ToLower().Contains("SAIGON EYE") is NOT returned (different seed) |
| TC-SCD-05 | `Process_EmptySeedData_ReturnsSuccessWithEmptyLists` | P12 | Both repos return empty → response has `Data.Clinics = []` and `Data.Doctors = []`. Covers `SearchClinicsAsync` + `SearchDoctorsAsync` empty paths |
| TC-SCD-06 | `Process_HappyPath_MapsClinicFieldsCorrectly` | P11 | Single clinic seed with all fields → asserts every `ClinicSearchItem` projection field: Id, Name, Address, Phone, Email, LogoUrl, Description, RatingAvg, ReviewCount (with `?? 0`) |
| TC-SCD-07 | `Process_HappyPath_MapsDoctorFieldsCorrectly` | P7, P11 | Single doctor seed → asserts every `DoctorSearchItem` projection field: Id, FullName, AvatarUrl, Title, Specialty (when not null), ClinicName, ExperienceYears, Bio, RatingAvg, ReviewCount (with `?? 0`) |
| TC-SCD-08 | `Process_DoctorWithNullSpecialty_MapSpecialtyAsNull` | P8 | Doctor seed with `Specialty = null` → response `Specialty == null`. Covers ternary `Specialty != null ? ... : null` false branch |
| TC-SCD-09 | `Process_ClinicWithNullReviewCount_DefaultsToZero` | P10 | Clinic with `ReviewCount = null` → response `ReviewCount == 0`. Covers `?? 0` branch on clinic |
| TC-SCD-10 | `Process_DoctorWithNullReviewCount_DefaultsToZero` | P9 | Doctor with `ReviewCount = null` → response `ReviewCount == 0`. Covers `?? 0` branch on doctor |
| TC-SCD-11 | `Process_OrdersClinicsAndDoctorsByNameAscending` | P11 | Seeds 2 clinics + 2 doctors OUT-OF-ORDER (e.g., "Zeta Clinic", "Alpha Clinic") and asserts the response items are alphabetically sorted by `Name`/`FullName`. Covers `OrderBy(c => c.Name)` + `OrderBy(d => d.User.FullName)` |
| TC-SCD-12 | `Process_ResponseShape_NoMetaOnSuccess` | P11 | Asserts `result.Meta == null` because `CreateSuccessResponse` uses the 2-arg `Success(codeMessage, data)` overload (no meta arg) |
| TC-SCD-13 | `Process_HappyPath_BothReposInvokedExactlyOnce` | P11 | Asserts `_clinicQueryRepository.FindByCondition(...)` and `_doctorQueryRepository.FindByCondition(...)` are called exactly once each |
| TC-SCD-14 | `Process_KeywordMatchesCaseInsensitiveOnClinicName` | P4 | Seed 2 clinics: "Saigon Eye" and "HA NOI Clinic"; keyword `"saigon eye"` (lowercase) → only "Saigon Eye" returned. Verifies `c.Name.ToLower().Contains(keyword)` semantics |

**Test arrangement**

```
ECS.Test/
└── Services/
    └── ClinicDoctorDiscoveryServices/                  ← existing test folder
        └── SearchClinicDoctorServices/
            └── SearchClinicDoctorServiceTests.cs       ← new file (14 tests)
```

---

## Cleanup

No stray `implementation_plan*.md` exists in the project root. ✅

---

## Test plan summary

- **Total tests**: 14
- **Frameworks**: xUnit + Moq + FluentAssertions + MockQueryable.Moq (all already installed).
- **Coverage tools**: `coverlet.collector 6.0.4` (installed) + `dotnet-reportgenerator-globaltool 5.5.10` (installed).
- **Execution**: `ECS.Test\run-tests.bat` — produces `TestResults\coverage-report\index.html`.

---

## Feedback request

`request_feedback = true` — awaiting user approval to proceed to implementation.