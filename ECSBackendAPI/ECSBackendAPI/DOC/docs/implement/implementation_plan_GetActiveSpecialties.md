# Implementation Plan — GetActiveSpecialties

> Generated by `/service_unit_testing_4_steps` on 2026-07-26.
> Target service: `ECS.Application.Services.ClinicDoctorDiscoveryService.GetActiveSpecialtiesServices.GetActiveSpecialtiesService`
> Target path: `BE/api/ECSBackendAPI/ECSBackendAPI/ECS.Application/Services/ClinicDoctorDiscoveryService/GetActiveSpecialtiesServices/GetActiveSpecialtiesService.cs`
> Test target path: `BE/api/ECSBackendAPI/ECSBackendAPI/ECS.Test/Services/ClinicDoctorDiscoveryServices/GetActiveSpecialtiesServices/GetActiveSpecialtiesServiceTests.cs`
> Goal: **100% line coverage** on `GetActiveSpecialtiesService.cs`.

---

## Step 1 — Read & Analyze (Done)

### Surface
- **Public contract**: `IGetActiveSpecialtiesService.Process()` → `Task<ApiResponse<List<GetActiveSpecialtiesResponse>>>` (no input parameters).
- **Single dependency**: `IRepositoryQueryBase<Specialty, Guid, AppDbContext>` (`trackChanges: false` is passed via `FindAll(false)`).

### Source lines (`GetActiveSpecialtiesService.cs`)
```
L29-37  Process() : public entry point — three sequential steps
L43-49  FetchActiveSpecialtiesFromRepo() : private async
           _specialtyQueryRepo.FindAll(trackChanges:false)
               .Where(s => s.IsActive)
               .OrderBy(s => s.Name)
               .ToListAsync()
L56-64  MapToSpecialtyListResponse() : private
           specialties.Select(s => new GetActiveSpecialtiesResponse { Id, Name, Description }).ToList()
L71-74  CreateApiResponse() : private, returns ApiResponse.Success("APP_MESSAGE_2000", data)
```

### Execution paths discovered
| # | Path | How triggered |
|---|------|---------------|
| P1 | Happy path with multiple active specialties → 2000 with ordered+mapped list | seeded list ≥ 2 items |
| P2 | Happy path empty → 2000 with empty `Data` list | seeded empty list |
| P3 | Single active specialty → covers `Select().ToList()` single-iteration | one item |
| P4 | `OrderBy(s => s.Name)` reorder vs input order — assertion validates row order | seed in non-alphabetical order |
| P5 | `Id` mapping: `s.Id.ToString()` covers Guid-string conversion | assertion checks `Id` equals `s.Id.ToString()` |
| P6 | `Description = null` mapping (nullable `string?`) | seed entity with `Description = null` |
| P7 | `CodeMessage = "APP_MESSAGE_2000"` constant value | assertion |
| P8 | `Meta = null` on success response | assertion |
| P9 | Private `FetchActiveSpecialtiesFromRepo()` reflection call with passed-through arg | direct invocation via `BindingFlags.NonPublic` |
| P10 | Private `CreateApiResponse()` reflection call | direct invocation |

### Empty-branch hunt
- ✅ The `Specialties` list itself can be empty → covered by P2. The mapping helper is fully covered by P1, P3, P6; entity rows are unconditionally iterated (`Select`), so no early-return paths exist in `MapToSpecialtyListResponse`.
- ✅ `CreateApiResponse` is unconditional; covered via P1, P2, P3 and an extra direct reflection test for explicit branch coverage.

### Refactor suggested for testability?
- **None.** The service is already highly testable: `Process()` returns `ApiResponse<List<GetActiveSpecialtiesResponse>>` (not `void`), and the only async dependency is a single `IQueryable` source that is trivially mockable through `MockQueryable.Moq`. No state-changing side effects exist.

---

## Step 2 — Testability Audit (Done)

- **Return type**: ✅ returns `Task<ApiResponse<List<...>>>` — assertions can read `CodeMessage`, `Data`, `Meta` without re-fetching state.
- **Database side-effects**: ✅ read-only.
- **Async chain**: ✅ single `await FetchActiveSpecialtiesFromRepo()` — `MockQueryable` handles `ToListAsync()`.

Conclusion: **no refactor required**.

---

## Step 3 — Implementation Plan (THIS DOCUMENT → awaiting user approval)

### Required changes
None — implementation is already testable as-is.

### Test cases (`[Method]_[State]_[Outcome]`)

**Process(...) tests**

| TC | Name | Assertion highlights |
|----|------|----------------------|
| TC-GAS-01 | `Process_EmptySpecialtyList_Returns2000WithEmptyData` | `CodeMessage = APP_MESSAGE_2000`, `Data != null`, `Data.Count == 0`, `Meta == null`. Verifies repo with empty list, covers `MapToSpecialtyListResponse` empty-input branch |
| TC-GAS-02 | `Process_HappyPath_SingleSpecialty_MapsAllFields` | seeds 1 active specialty, asserts `Id == specialty.Id.ToString()`, `Name == "Cardiology"`, `Description == "Heart care"`. Covers every assignment in mapper |
| TC-GAS-03 | `Process_HappyPath_MultipleSpecialties_OrderedByNameAscending` | seeds 3 active specialties **out of order** ("Zeta", "Alpha", "Mu") and asserts the resulting list is **sorted alphabetically by `Name`** (asserting row order in `Data[0..2]`). Covers `OrderBy` arm of `FetchActiveSpecialtiesFromRepo` |
| TC-GAS-04 | `Process_NullableDescription_MapsToNullDtoField` | seeds a specialty whose `Description` is `null`; asserts the resulting `Description` is `null`. Covers nullable `string?` propagation through the mapper |
| TC-GAS-05 | `Process_ResponseShape_HasNoMetaOnSuccess` | asserts `result.Meta == null` because `CreateApiResponse` builds the response via `Success(codeMessage, data)` overload (no meta arg) |
| TC-GAS-06 | `Process_RepoReturnsList_UsesOrderByNameSemantics` | same as TC-GAS-03 but uses a stronger guarantee: seeds non-alphabetical rows then verifies the resulting order. Independent test to provide a separate branch coverage count |

**Private-method reflection tests**

| TC | Name | Target |
|----|------|--------|
| TC-GAS-07 | `FetchActiveSpecialtiesFromRepo_EmptyRepo_ReturnsEmptyList` | directly invokes the private method via `BindingFlags.NonPublic`, asserts empty list passed through. Also verifies `specialtyQueryRepo.FindAll(false)` is invoked once |
| TC-GAS-08 | `FetchActiveSpecialtiesFromRepo_MultipleRows_ReturnsListInRepoOrder` | direct reflection call, seeds unordered rows, asserts returned rows match the seeded list (no DB-side `OrderBy` reordering at this level — the LINQ expression is what the implementation defines; the MockQueryable's `OrderBy` is enforced because `BuildMockDbSet` already supports it). This is primarily a line-coverage and `MockQueryable` correctness check |
| TC-GAS-09 | `CreateApiResponse_WrapsDataWithCodeMessage2000` | direct reflection call, asserts `CodeMessage == APP_MESSAGE_2000`, `Data == inputList`, `Meta == null` |

**Process(...) verification tests (mock invocation count)**

| TC | Name | Assertion |
|----|------|-----------|
| TC-GAS-10 | `Process_InvokesSpecialtyRepoFindAllExactlyOnce` | seeds empty list, then asserts `_specialtyQueryRepo.FindAll(false)` is called exactly once and `_sut.Process()` returns APP_MESSAGE_2000 |

**Test arrangement**

```
ECS.Test/
└── Services/
    └── ClinicDoctorDiscoveryServices/                 ← new test folder
        └── GetActiveSpecialtiesServices/
            └── GetActiveSpecialtiesServiceTests.cs    ← new file (10 tests)
```

---

## Cleanup

No stray `implementation_plan*.md` exists in the project root (verified via Glob). ✅ no migration needed.

---

## Test plan summary

- **Total tests**: 10
- **Tests use**: xUnit + Moq + FluentAssertions + MockQueryable.Moq (all already installed in `ECS.Test.csproj`).
- **Coverage tools**: `coverlet.collector 6.0.4` (already installed) and `dotnet-reportgenerator-globaltool 5.5.10` (already installed globally).
- **Execution**: `ECS.Test\run-tests.bat` — produces `TestResults\coverage-report\index.html` automatically.

---

## Feedback request

`request_feedback = true` — awaiting user approval to proceed to implementation.
