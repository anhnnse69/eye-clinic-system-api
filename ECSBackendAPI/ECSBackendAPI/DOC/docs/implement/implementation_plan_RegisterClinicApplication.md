# Implementation Plan — RegisterClinicApplication

> Generated by `/service_unit_testing_4_steps` on 2026-07-26.
> Target service: `ECS.Application.Services.ClinicDoctorDiscoveryService.RegisterClinicApplicationServices.RegisterClinicApplicationService`
> Target path: `BE/api/ECSBackendAPI/ECSBackendAPI/ECS.Application/Services/ClinicDoctorDiscoveryService/RegisterClinicApplicationServices/RegisterClinicApplicationService.cs`
> Test target path: `BE/api/ECSBackendAPI/ECSBackendAPI/ECS.Test/Services/ClinicDoctorDiscoveryServices/RegisterClinicApplicationServices/RegisterClinicApplicationServiceTests.cs`
> Goal: **100% line coverage** on `RegisterClinicApplicationService.cs`.

---

## Step 1 — Read & Analyze (Done)

### Surface
- **Public contract**: `IRegisterClinicApplicationService.Process(RegisterClinicApplicationRequest request)` → `Task<ApiResponse<bool>>`.
- **Dependencies (3)**:
  1. `IRepositoryQueryBase<ClinicRegistrationRequest, Guid, AppDbContext>` — used by `CheckPendingApplicationAsync` to query existing PENDING rows by email.
  2. `IRepositoryBaseAsync<ClinicRegistrationRequest, Guid, AppDbContext>` — used by `CreateClinicApplicationAsync` for `CreateAsync(...)` + `SaveChangesAsync()`.
  3. `IValidator<RegisterClinicApplicationRequest>` — wraps `RegisterClinicApplicationRequestValidator` (FluentValidation).

### Source lines (`RegisterClinicApplicationService.cs`)
```
L46-55  Process(request) : public entry point — three sequential steps
L61-72  ValidateRequestAsync(request) : private async — wraps _validator.ValidateAsync
L78-94  CheckPendingApplicationAsync(request, isValidationPassed) : private async — early-exit when invalid
L100-113 CreateResponse(validationResult, duplicateResult, request) : private async — composes the API response
L119-134 CreateErrorResponse(validationResult, duplicateResult) : private — pure, returns null when no error
L140-151 CreateClinicApplicationAsync(request) : private async — persists + returns success
L157-172 BuildClinicApplication(request) : private static — entity mapping (string trim/lower normalisation)
L177, L182 Private nested records: ValidationResult / DuplicateResult (no behaviour)
```

### Execution paths discovered
| # | Path | How triggered |
|---|------|---------------|
| P1 | Validator returns invalid → `ValidationResult(false, errCode)` → `CreateErrorResponse` returns `Fail(errCode)` → `Process` short-circuits before persistence | validator mock returns invalid |
| P2 | Validator valid, no pending duplicate → `DuplicateResult(false)` → `CreateResponse` calls `CreateClinicApplicationAsync` → repo `CreateAsync` + `SaveChangesAsync` → success `APP_MESSAGE_2000` | validator valid, repo FindByCondition returns empty |
| P3 | Validator valid, duplicate pending exists → `DuplicateResult(true)` → `CreateErrorResponse` returns `Fail(APP_MESSAGE_4023)` | validator valid, repo FindByCondition returns non-empty |
| P4 | `BuildClinicApplication` mapping (clinic/contact name+address+phone trim, email lower, status="PENDING", RequestedAt=DateTime.UtcNow) | covered via P2's `CreateAsync` invocation assertion |
| P5 | `Process` calls `ValidateRequestAsync` → `CheckPendingApplicationAsync` → `CreateResponse` in order | end-to-end happy path |

### Empty-branch hunt
- ✅ `ValidationResult` validation-passed branch (`if (!result.IsValid)` true/false) — covered by P1 + P2.
- ✅ `CheckPendingApplicationAsync` `if (!isValidationPassed)` early-return branch — covered by P1.
- ✅ `CheckPendingApplicationAsync` repository query branch — covered by P2 + P3.
- ✅ `CreateResponse` `if (errorResponse != null)` branch — covered by P1, P2, P3.
- ✅ `CreateErrorResponse` `if (!validationResult.IsPassed)` branch — covered by P1.
- ✅ `CreateErrorResponse` `if (duplicateResult.Exists)` branch — covered by P3.
- ✅ `CreateErrorResponse` default `return null` branch — covered by P2.
- ✅ `CreateClinicApplicationAsync` `BuildClinicApplication` + repo write path — covered by P2.

### Refactor suggested for testability?
- **None.** `Process` already returns `Task<ApiResponse<bool>>`. All repo methods used (`FindByCondition`, `CreateAsync`, `SaveChangesAsync`) are mockable. The validator is injected and trivially mockable.

---

## Step 2 — Testability Audit (Done)

- **Return type**: ✅ `Task<ApiResponse<bool>>` is fully inspectable.
- **Async chain**: ✅ all awaits consume simple mocks; no hidden side-effects.
- **Mocking surface**: ✅ all 3 dependencies are interfaces — easy to mock.

Conclusion: **no refactor required**.

---

## Step 3 — Implementation Plan (THIS DOCUMENT → awaiting user approval)

### Required changes
None.

### Test cases (`[Method]_[State]_[Outcome]`)

**Process(...) tests** (covers `Process`, `ValidateRequestAsync`, `CheckPendingApplicationAsync`, `CreateResponse`, `CreateErrorResponse`, `CreateClinicApplicationAsync`, `BuildClinicApplication`)

| TC | Name | Path | Assertion highlights |
|----|------|------|----------------------|
| TC-RCA-01 | `Process_ValidationFails_ReturnsErrorCodeWithoutQuerying` | P1 | validator mock returns `IsValid=false` with errCode → response `CodeMessage == errCode`, `Data == null`. Verifies `_clinicApplicationQueryRepository.FindByCondition(...)` is **never** called and `CreateAsync`/`SaveChangesAsync` are **never** called. Covers `ValidateRequestAsync` invalid branch + `CheckPendingApplicationAsync` early-return + `CreateErrorResponse` validation-fail branch |
| TC-RCA-02 | `Process_ValidationPassedNoDuplicate_PersistsAndReturnsSuccess` | P2 | validator valid; FindByCondition returns empty → `CreateAsync` invoked with `ClinicRegistrationRequest` having trimmed names and lowercased email, `Status == "PENDING"`. Verifies `SaveChangesAsync` invoked once and `CodeMessage == APP_MESSAGE_2000`, `Data == true`. Covers `BuildClinicApplication` mapping, `CreateClinicApplicationAsync` write path, `CreateErrorResponse` default-null branch |
| TC-RCA-03 | `Process_ValidationPassedDuplicatePending_Returns4023` | P3 | validator valid; FindByCondition returns non-empty → `CreateAsync`/`SaveChangesAsync` **never** called, `CodeMessage == APP_MESSAGE_4023`, `Data == null`. Covers `CheckPendingApplicationAsync` query branch + `CreateErrorResponse` duplicate branch |
| TC-RCA-04 | `Process_ValidationFailsWithCustomErrorCode_PropagatesErrorCode` | P1 | uses a custom error code (`APP_MESSAGE_4019`) → response `CodeMessage == APP_MESSAGE_4019` to prove the validator's error code propagates verbatim. Same path as TC-RCA-01 but with explicit code verification |
| TC-RCA-05 | `Process_DuplicateCheckEmailIsTrimmedAndLowercased` | P3 | request has email `"  Test@Clinic.VN  "` → service computes `normalizedEmail == "test@clinic.vn"`. Asserts `_clinicApplicationQueryRepository.FindByCondition(...)` is called (proving the trim/lowercase branch executed) |
| TC-RCA-06 | `Process_BuildClinicApplication_TrimsStringFieldsAndLowercasesEmail` | P2 | request has `"  MyClinic  "` (name), `"  Test@Clinic.VN  "` (email); asserts `CreateAsync` payload has `ClinicName == "MyClinic"`, `ContactEmail == "test@clinic.vn"`, `Status == "PENDING"`, `Id != Guid.Empty`. Covers every `BuildClinicApplication` mapping |
| TC-RCA-07 | `Process_HappyPath_BusinessLicenseUrlIsCopiedVerbatim` | P2 | request provides a `BusinessLicenseUrl`; asserts `CreateAsync` payload has the exact URL — covers the `BusinessLicenseUrl = request.BusinessLicenseUrl` mapping branch |

**Test arrangement**

```
ECS.Test/
└── Services/
    └── ClinicDoctorDiscoveryServices/                     ← new test folder
        └── RegisterClinicApplicationServices/
            └── RegisterClinicApplicationServiceTests.cs    ← new file (7 tests)
```

---

## Cleanup

No stray `implementation_plan*.md` exists in the project root. ✅

---

## Test plan summary

- **Total tests**: 7
- **Frameworks**: xUnit + Moq + FluentAssertions + MockQueryable.Moq (all already installed).
- **Coverage tools**: `coverlet.collector 6.0.4` (installed) + `dotnet-reportgenerator-globaltool 5.5.10` (installed).
- **Execution**: `ECS.Test\run-tests.bat` — produces `TestResults\coverage-report\index.html`.

---

## Feedback request

`request_feedback = true` — awaiting user approval to proceed to implementation.