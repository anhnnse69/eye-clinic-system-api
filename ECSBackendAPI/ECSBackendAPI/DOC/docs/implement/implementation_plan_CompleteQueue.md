# Implementation Plan — CompleteQueue

> Generated by `/service_unit_testing_4_steps` on 2026-07-26.
> Target service: `ECS.Application.Services.DoctorAppointmentPatientManagementServices.CompleteQueueServices.CompleteQueueService`
> Target path: `BE/api/ECSBackendAPI/ECSBackendAPI/ECS.Application/Services/DoctorAppointmentPatientManagementServices/CompleteQueueServices/CompleteQueueService.cs`
> Test target path: `BE/api/ECSBackendAPI/ECSBackendAPI/ECS.Test/Services/DoctorAppointmentPatientManagementServices/CompleteQueueServices/CompleteQueueServiceTests.cs`
> Goal: **100% line coverage** on `CompleteQueueService.cs`.

---

## Step 1 — Read & Analyze (Done)

### Surface
- **Public contract**: `ICompleteQueueService.Process(CompleteQueueRequest request)` → `Task<ApiResponse<CompleteQueueResponse>>`.
- **Dependencies (5)**:
  1. `IRepositoryQueryBase<Queue, Guid, AppDbContext>` — used by `GetQueueAsync` (`FindByCondition().Include(Appointment).FirstOrDefaultAsync()`).
  2. `IRepositoryQueryBase<Appointment, Guid, AppDbContext>` — used by `GetAppointmentAsync` (`FindByCondition().Include(Patient, PreliminaryDiagnosis, MedicalRecord).FirstOrDefaultAsync()`).
  3. `IValidator<CompleteQueueRequest>` — used by `ValidateRequest`.
  4. `AppDbContext` — used by `CompleteQueueAsync` (`Queues.Update`, `Appointments.Update`, `SaveChangesAsync`).
  5. `IHttpContextAccessor` — used by `RetrieveAuthenticatedUserId` (`HttpContext?.User.FindFirst(ClaimTypes.NameIdentifier)?.Value`).
- **Exception path**: Service does NOT throw exceptions. All failure paths set `state.HasError = true` and `state.ErrorCode = ...`, then `CreateResponse` returns `ApiResponse.Fail`.

### Source lines (`CompleteQueueService.cs`)
```
L42-61   Process(request) : public entry — 8 sequential steps (with `HasError` early-return guards)
L68-83   ExecutionState : private nested class with 11 properties
L92-98   ValidateRequest(request, state) : void — FluentValidator → state flags
L103-111 RetrieveAuthenticatedUserId(state) : void — HttpContext NameIdentifier → Guid.TryParse → state
L116-122 ParseQueueId(queueId, state) : void — Guid.TryParse → state
L127-138 GetQueueAsync(state) : Task — early-return if HasError; otherwise repo.FindByCondition().Include(Appointment).FirstOrDefaultAsync()
L143-156 GetAppointmentAsync(state) : Task — early-return if HasError || Queue == null
L162-175 VerifyMedicalRecordExists(state) : void — early-return if HasError || Queue == null || Appointment == null; checks Appointment.PreliminaryDiagnosis != null; Console.WriteLine debug log
L184-212 CompleteQueueAsync(state) : Task — early-return if HasError || Queue == null; try-block sets Queue.Status = COMPLETED, calls _context.Queues.Update, conditionally _context.Appointments.Update, awaits _context.SaveChangesAsync; catch sets IsExecutionSuccess=false, HasError=true, ErrorCode=APP_MESSAGE_5001
L221-240 CreateResponse(state) : instance method — if HasError → ApiResponse.Fail; else ApiResponse.Success(APP_MESSAGE_2007, payload)
```

### Execution paths discovered

| # | Path | How triggered |
|---|------|---------------|
| P1 | Happy path | valid Guid queueId + valid Guid user + queue found + appointment found + PreliminaryDiagnosis exists + SaveChanges succeeds → success |
| P2 | Validation fails (empty QueueId) | request.QueueId == "" → validator.NotEmpty fails → state.HasError=true, ErrorCode=APP_MESSAGE_4019 → all subsequent steps early-return |
| P3 | Validation fails (non-Guid QueueId) | request.QueueId == "not-a-guid" → validator.BeAValidGuid fails → same as P2 |
| P4 | Auth failure (HttpContext null) | no HttpContext wired → principalIdValue = null → parseResult=false → state.HasError=true, ErrorCode=APP_MESSAGE_4033 |
| P5 | Auth failure (invalid Guid claim) | claim value is non-Guid string → parseResult=false → same as P4 |
| P6 | `ParseQueueId` parseResult=true branch | valid Guid QueueId → QueueId set |
| P7 | `ParseQueueId` parseResult=false branch | invalid Guid QueueId (e.g., "not-a-guid") → QueueId = Guid.Empty, ErrorCode=APP_MESSAGE_4019 |
| P8 | `GetQueueAsync` repo returns empty → IsQueueValid=false | empty Queue repo → state.ErrorCode=APP_MESSAGE_4052 |
| P9 | `GetQueueAsync` early-return when HasError | prior step set HasError → no repo call |
| P10 | `GetAppointmentAsync` repo returns empty → IsAppointmentValid=false | empty Appointment repo → state.ErrorCode=APP_MESSAGE_4012 |
| P11 | `VerifyMedicalRecordExists` PreliminaryDiagnosis exists → IsMedicalRecordValid=true | appointment with non-null PreliminaryDiagnosis |
| P12 | `VerifyMedicalRecordExists` PreliminaryDiagnosis null → IsMedicalRecordValid=false | appointment with null PreliminaryDiagnosis → state.ErrorCode=APP_MESSAGE_4028 |
| P13 | `CompleteQueueAsync` happy path (queue not COMPLETED) | Status set, Queues.Update called, Appointments.Update called, SaveChangesAsync called |
| P14 | `CompleteQueueAsync` Appointment already COMPLETED → skipped | Status check skips appointment update (only Queues.Update called) |
| P15 | `CompleteQueueAsync` SaveChangesAsync throws → caught | state.ErrorCode=APP_MESSAGE_5001 |
| P16 | `CompleteQueueAsync` early-return (HasError or Queue==null) | does nothing |
| P17 | `CreateResponse` HasError=true → Fail branch | any failure path |
| P18 | `CreateResponse` HasError=false → Success branch | full happy path |
| P19 | `CreateResponse` Success — every field mapped | queue + appointment with Patient.FullName |

### Empty-branch hunt
- ✅ `ParseQueueId` `parseResult` true branch — P6 (via reflection or happy path)
- ✅ `ParseQueueId` `parseResult` false branch — P7 (via reflection)
- ✅ `ValidateRequest` `result.IsValid` true → null ErrorCode — P1
- ✅ `ValidateRequest` `result.IsValid` false → APP_MESSAGE_4019 — P2, P3
- ✅ `RetrieveAuthenticatedUserId` true branch — P1
- ✅ `RetrieveAuthenticatedUserId` false branch — P4, P5
- ✅ `GetQueueAsync` early-return guard — P9
- ✅ `GetQueueAsync` queue not null branch — P1
- ✅ `GetQueueAsync` queue null branch — P8
- ✅ `GetAppointmentAsync` early-return guard — P2+P3 (HasError) + P8 (Queue==null)
- ✅ `GetAppointmentAsync` appointment not null branch — P1
- ✅ `GetAppointmentAsync` appointment null branch — P10
- ✅ `VerifyMedicalRecordExists` early-return guard — covered by P2/P3/P8/P10
- ✅ `VerifyMedicalRecordExists` PreliminaryDiagnosis not null — P1
- ✅ `VerifyMedicalRecordExists` PreliminaryDiagnosis null — P12
- ✅ `CompleteQueueAsync` early-return guard — P2, P3, P4, P5, P8, P10, P12
- ✅ `CompleteQueueAsync` try happy path with appointment not yet COMPLETED — P13
- ✅ `CompleteQueueAsync` try happy path with appointment already COMPLETED → skips appointment update — P14
- ✅ `CompleteQueueAsync` catch block — P15
- ✅ `CreateResponse` Fail branch — P17 (any failure path)
- ✅ `CreateResponse` Success branch — P1

### Mocking strategy
- `IHttpContextAccessor` — standard Moq.
- `IValidator<CompleteQueueRequest>` — use a real `CompleteQueueRequestValidator()` instance; for invalid cases, we can use a stub via Moq or directly the validator.
- `IRepositoryQueryBase<Queue, AppDbContext>` and `<Appointment, AppDbContext>` — MockQueryable.
- **`AppDbContext`** — because `Queues`/`Appointments` properties are NOT marked `virtual`, we cannot easily Mock them. Solution: use **EF Core In-Memory provider** with a real `AppDbContext` instance. The provider supports `Include()`, `FirstOrDefaultAsync()`, `Update()`, `SaveChangesAsync()` — all needed.

### Refactor suggested for testability?
- **No refactor** needed for testability. However, we can suppress `Console.WriteLine` in test setup to keep test output clean.

---

## Step 2 — Testability Audit (Done)

- **Return type**: ✅ `Task<ApiResponse<CompleteQueueResponse>>`.
- **Async chain**: ✅ all awaits work with In-Memory EF Core.
- **Mocking surface**: ✅ HttpContext + Validator via Moq; Repositories via MockQueryable; AppDbContext via In-Memory EF Core.
- **Exception path**: ✅ no exception throws; all failures go through `state.ErrorCode` and `ApiResponse.Fail`.

Conclusion: **no refactor required**.

---

## Step 3 — Implementation Plan (THIS DOCUMENT → awaiting user approval)

### Required changes
None.

### Test cases (`[Method]_[State]_[ExpectedResult]`)

**Process(...) tests**

| TC | Name | Path | Assertion highlights |
|----|------|------|----------------------|
| TC-CQ-01 | `Process_HappyPath_Returns2007SuccessWithAllFields` | P1, P13, P18, P19 | Valid Guid + valid user + queue + appointment + PreliminaryDiagnosis → response.CodeMessage=APP_MESSAGE_2007, response.Data populated (QueueId, AppointmentId, PatientName, QueueNumber, PreviousStatus, CompletedAt, IsSuccess=true), _context.Queues contains 1 entry with Status=COMPLETED |
| TC-CQ-02 | `Process_EmptyQueueId_Returns4019ValidationFail` | P2, P9, P17 | request.QueueId="" → Fail(APP_MESSAGE_4019), Data=null, queue/appointment repos never called |
| TC-CQ-03 | `Process_NonGuidQueueId_Returns4019ValidationFail` | P3, P7, P9, P17 | request.QueueId="not-a-guid" → Fail(APP_MESSAGE_4019) |
| TC-CQ-04 | `Process_NullHttpContext_Returns4033AuthFail` | P4, P17 | HttpContext null → Fail(APP_MESSAGE_4033) |
| TC-CQ-05 | `Process_InvalidGuidClaim_Returns4033AuthFail` | P5, P17 | HttpContext claim value="not-a-guid" → Fail(APP_MESSAGE_4033) |
| TC-CQ-06 | `Process_QueueNotFound_Returns4052QueueFail` | P8, P17 | valid input + empty Queue repo → Fail(APP_MESSAGE_4052), appointment repo never called |
| TC-CQ-07 | `Process_AppointmentNotFound_Returns4012AppointmentFail` | P10, P17 | valid input + queue found + empty Appointment repo → Fail(APP_MESSAGE_4012) |
| TC-CQ-08 | `Process_MissingPreliminaryDiagnosis_Returns4028MedicalRecordFail` | P12, P17 | valid input + queue + appointment with null PreliminaryDiagnosis → Fail(APP_MESSAGE_4028) |
| TC-CQ-09 | `Process_SaveChangesAsyncThrows_Returns5001InternalError` | P15, P17 | set up `_context.SaveChangesAsync` to throw → Fail(APP_MESSAGE_5001), IsExecutionSuccess=false |
| TC-CQ-10 | `Process_AppointmentAlreadyCompleted_SkipsAppointmentUpdate` | P14 | appointment.Status=COMPLETED → appointment Update never called; queue Update called |
| TC-CQ-11 | `Process_QueueEarlyReturn_WhenHasErrorInPriorStep` | P9 | combined: validation fails → GetQueueAsync never calls repo (verify) |

**Private-method reflection tests**

| TC | Name | Target |
|----|------|--------|
| TC-CQ-12 | `ValidateRequest_ValidRequest_SetsIsValidationPassed` | direct reflection call, valid Guid → state.IsValidationPassed=true, HasError=false, ErrorCode=null |
| TC-CQ-13 | `ValidateRequest_EmptyQueueId_SetsErrorCode4019` | direct reflection call, empty → IsValidationPassed=false, ErrorCode=APP_MESSAGE_4019 |
| TC-CQ-14 | `ValidateRequest_NonGuidQueueId_SetsErrorCode4019` | direct reflection call, "not-a-guid" → ErrorCode=APP_MESSAGE_4019 |
| TC-CQ-15 | `RetrieveAuthenticatedUserId_ValidGuid_SetsIsUserValid` | direct reflection call, valid Guid claim → IsUserValid=true, ActiveUserId=expected |
| TC-CQ-16 | `RetrieveAuthenticatedUserId_NullContext_SetsErrorCode4033` | direct reflection call, no HttpContext → IsUserValid=false, ErrorCode=APP_MESSAGE_4033 |
| TC-CQ-17 | `RetrieveAuthenticatedUserId_InvalidGuid_SetsErrorCode4033` | direct reflection call, "not-a-guid" claim → IsUserValid=false |
| TC-CQ-18 | `ParseQueueId_ValidGuid_SetsQueueId` | direct reflection call, valid Guid → state.QueueId=parsed, HasError=false (assuming no prior error) |
| TC-CQ-19 | `ParseQueueId_InvalidGuid_SetsErrorCode4019` | direct reflection call, "not-a-guid" → state.QueueId=Guid.Empty, ErrorCode=APP_MESSAGE_4019 |
| TC-CQ-20 | `ParseQueueId_PreservesPriorErrorCode_WhenParseSucceeds` | direct reflection call, pre-set HasError=true + valid Guid → ErrorCode preserved |
| TC-CQ-21 | `VerifyMedicalRecordExists_PreliminaryDiagnosisExists_SetsValid` | direct reflection call, appointment with PreliminaryDiagnosis → IsMedicalRecordValid=true |
| TC-CQ-22 | `VerifyMedicalRecordExists_PreliminaryDiagnosisNull_SetsErrorCode4028` | direct reflection call, appointment with null → ErrorCode=APP_MESSAGE_4028 |
| TC-CQ-23 | `VerifyMedicalRecordExists_EarlyReturn_WhenHasError` | direct reflection call, pre-set HasError=true → state unchanged |
| TC-CQ-24 | `VerifyMedicalRecordExists_EarlyReturn_WhenAppointmentNull` | direct reflection call, state.Appointment=null → no-op |
| TC-CQ-25 | `CreateResponse_HasError_ReturnsFail` | direct reflection call, state.HasError=true → ApiResponse.Fail with ErrorCode |
| TC-CQ-26 | `CreateResponse_HappyPath_ReturnsSuccessWith2007` | direct reflection call, valid state → ApiResponse.Success(APP_MESSAGE_2007, payload), every field mapped |

**Test arrangement**

```
ECS.Test/
└── Services/
    └── DoctorAppointmentPatientManagementServices/
        └── CompleteQueueServices/
            └── CompleteQueueServiceTests.cs                   ← new file (26 tests)
```

---

## Cleanup

No stray `implementation_plan*.md` exists in the project root. ✅

---

## Test plan summary

- **Total tests**: 26
- **Frameworks**: xUnit + Moq + FluentAssertions + MockQueryable.Moq + FluentValidation (real instance) + EF Core In-Memory + Reflection helpers (all already in convention).
- **Coverage tools**: `coverlet.collector 6.0.4` (installed) + `dotnet-reportgenerator-globaltool 5.5.10` (installed).
- **Execution**: `ECS.Test\run-tests.bat` — produces `TestResults\coverage-report\index.html`.

---

## Feedback request

`request_feedback = true` — awaiting user approval to proceed to implementation.