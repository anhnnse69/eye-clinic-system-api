# Implementation Plan — ViewPatientDetail

> Generated by `/service_unit_testing_4_steps` on 2026-07-27.
> Target service: `ECS.Application.Services.DoctorAppointmentPatientManagementServices.ViewPatientDetailServices.ViewPatientDetailService`
> Target path: `BE/api/ECSBackendAPI/ECSBackendAPI/ECS.Application/Services/DoctorAppointmentPatientManagementServices/ViewPatientDetailServices/ViewPatientDetailService.cs`
> Test target path: `BE/api/ECSBackendAPI/ECSBackendAPI/ECS.Test/Services/DoctorAppointmentPatientManagementServices/ViewPatientDetailServices/ViewPatientDetailServiceTests.cs`
> Goal: **100% line coverage** on `ViewPatientDetailService.cs`.

---

## Step 1 — Read & Analyze (Done)

### Surface
- **Public contract**: `IViewPatientDetailService.Process(Guid userId, Guid patientId)` → `Task<ApiResponse<ViewPatientDetailResponse>>`.
- **Dependencies (3)**:
  1. `IRepositoryQueryBase<DoctorProfile, Guid, AppDbContext>` — queried by `ResolveActiveDoctorProfileAsync`.
  2. `IRepositoryQueryBase<PatientProfile, Guid, AppDbContext>` — queried by `FetchPatientProfileAsync`.
  3. `IRepositoryQueryBase<Appointment, Guid, AppDbContext>` — queried twice: by `EnsureDoctorPatientRelationshipAsync` (`.AnyAsync`) and `FetchAppointmentHistoryAsync` (`.ToListAsync`).

### Source lines (`ViewPatientDetailService.cs`)
```
L39-49   Process(userId, patientId)               : public entry point — 5 sequential steps
L53-63   ResolveActiveDoctorProfileAsync(userId)  : private async — FindByCondition + FirstOrDefaultAsync; throws 4008
L65-75   EnsureDoctorPatientRelationshipAsync     : private async — FindByCondition + AnyAsync; throws 4004 if no relation
L77-87   FetchPatientProfileAsync(patientId)      : private async — FindByCondition + Include(User) + FirstOrDefaultAsync; throws 4004 if null
L94-104  FetchAppointmentHistoryAsync             : private async — FindByCondition + Include(Service) + Include(MedicalRecord) + OrderByDescending + ToListAsync
L106-126 BuildResponse(patient, appointments)     : private static — maps patient + list of appointments
L128-139 MapAppointmentItem(appointment)          : private static — single appointment mapping
L146-162 MapMedicalRecord(mr)                     : private static — returns null if mr is null; else builds summary
L164-169 CreateSuccessResponse(response)          : private static — wraps in ApiResponse<T>.Success
```

### Execution paths discovered
| # | Path | How triggered |
|---|------|---------------|
| P1 | Doctor profile not found / inactive → throws `KeyNotFoundException(APP_MESSAGE_4008)` | `_doctorRepo.FindByCondition(...).FirstOrDefaultAsync() == null` |
| P2 | No doctor-patient relationship (no matching appointment) → throws `KeyNotFoundException(APP_MESSAGE_4004)` | `_appointmentRepo.FindByCondition(...).AnyAsync() == false` |
| P3 | Patient profile not found → throws `KeyNotFoundException(APP_MESSAGE_4004)` | `_patientRepo.FindByCondition(...).FirstOrDefaultAsync() == null` |
| P4 | Happy path: doctor + relation + patient + appointments → builds response | All repos return non-empty data |
| P5 | Patient exists but no appointments → `appointments = []` → response `Appointments = []` | `_appointmentRepo.FindByCondition(...).ToListAsync() == []` |
| P6 | Appointment has `MedicalRecord == null` → `MapMedicalRecord` returns null → `AppointmentHistoryItem.MedicalRecord = null` | seed appointment without medical record |
| P7 | Appointment has non-null `MedicalRecord` → `MapMedicalRecord` builds `MedicalRecordSummary` with `Prescriptions = []` | seed appointment with medical record |
| P8 | Patient has `User == null` → `BuildResponse` maps `AvatarUrl = null` (null-conditional) | seed patient without user |
| P9 | Patient has User with AvatarUrl → mapped | seed patient with user |
| P10 | Appointment has `Service == null` → `ServiceName = null` (null-conditional) | seed appointment without service |
| P11 | Status.ToString() always called → string mapping | any Appointment |
| P12 | `CreateSuccessResponse` always wraps `APP_MESSAGE_2000` + data + Meta null | any success path |

### Empty-branch hunt
- ✅ `ResolveActiveDoctorProfileAsync` null branch → P1.
- ✅ `EnsureDoctorPatientRelationshipAsync` `!hasRelation` branch → P2.
- ✅ `FetchPatientProfileAsync` null branch → P3.
- ✅ `MapMedicalRecord` null branch → P6.
- ✅ `MapMedicalRecord` non-null branch → P7.
- ✅ `BuildResponse` `patient.User?.AvatarUrl` null branch → P8.
- ✅ `MapAppointmentItem` `appointment.Service?.ServiceName` null branch → P10.
- ✅ `BuildResponse` appointments empty list branch → P5 (handled by LINQ `.Select().ToList()` returning empty list).

### Refactor suggested for testability?
- **None.** The service is read-only, returns `ApiResponse<ViewPatientDetailResponse>`, and has no side-effects. All 3 dependencies are interfaces. `MockQueryable.Moq` provides `BuildMockDbSet<T>()` which honors `Where`, `FirstOrDefaultAsync`, `AnyAsync`, `OrderByDescending`, `ToListAsync`; `.Include()` calls are no-ops but the projection still works because the seed rows are pre-populated with navigation property instances (`User`, `Service`, `MedicalRecord`).

---

## Step 2 — Testability Audit (Done)

- **Return type**: ✅ `Task<ApiResponse<ViewPatientDetailResponse>>` — fully inspectable.
- **Async chain**: ✅ all awaits consume simple mocks; private helpers are exercised via the public `Process` entry point (no need for reflection).
- **Mocking surface**: ✅ all 3 dependencies are interfaces.

Conclusion: **no refactor required**.

---

## Step 3 — Implementation Plan (THIS DOCUMENT → awaiting user approval)

### Required changes
None.

### MockData helper

`ECS.Test/MockData/ViewPatientDetailMockData.cs` (created):
- Constants: `DefaultDoctorUserId`, `DefaultDoctorProfileId`, `DefaultPatientId`, `DefaultPatientUserId`, `DefaultAppointmentId`, `DefaultMedicalRecordId`, `DefaultServiceId`.
- `GetDoctorProfile(id?, userId?, isActive?)` → returns a fully populated `DoctorProfile`.
- `GetUser(id?, avatarUrl?)` → returns a fully populated `User`.
- `GetPatientProfile(id?, user?, avatarUrl?)` → returns a fully populated `PatientProfile` with default avatar.
- `GetService(id?)` → returns a `Service` named "Eye Examination".
- `GetMedicalRecord(id?, recordType?)` → returns a fully populated `MedicalRecord` (RecordType defaults to `MS21_TRAUMA`).
- `GetAppointment(id?, doctorId?, patientId?, service?, medicalRecord?, appointmentDate?, status?, symptoms?)` → returns a fully populated `Appointment` with `Service` + `Patient` + `Doctor` navigation properties set.

### Test cases (`[Method]_[State]_[Outcome]`)

**Process(...) tests** (covers `Process`, `ResolveActiveDoctorProfileAsync`, `EnsureDoctorPatientRelationshipAsync`, `FetchPatientProfileAsync`, `FetchAppointmentHistoryAsync`, `BuildResponse`, `MapAppointmentItem`, `MapMedicalRecord`, `CreateSuccessResponse`)

| TC | Name | Path | Assertion highlights |
|----|------|------|----------------------|
| TC-VPD-01 | `Process_DoctorProfileMissingOrInactive_ThrowsDoctorNotFound` | P1 | Doctor repo returns empty → throws `KeyNotFoundException` with message `APP_MESSAGE_4008`. Patient repo + appointment repo not consulted |
| TC-VPD-02 | `Process_NoDoctorPatientRelationship_ThrowsRelationNotFound` | P2 | Doctor exists; appointment repo returns empty for relationship check → throws `KeyNotFoundException` with message `APP_MESSAGE_4004`. Patient repo not consulted |
| TC-VPD-03 | `Process_PatientProfileNotFound_ThrowsPatientNotFound` | P3 | Doctor + relation exist; patient repo returns empty → throws `KeyNotFoundException` with message `APP_MESSAGE_4004` |
| TC-VPD-04 | `Process_ValidDoctorPatientAndRelation_ReturnsSuccessWithResponse` | P4 | Doctor + patient + appointment (with service + medical record) → response `CodeMessage = APP_MESSAGE_2000`, `Data.PatientId == patient.Id`, `Data.FullName`, `Data.Gender`, `Data.Dob`, etc. |
| TC-VPD-05 | `Process_NoAppointments_ReturnsSuccessWithEmptyAppointmentList` | P5 | Doctor + patient + relation exist but appointment history list is empty → response `Data.Appointments` is empty |
| TC-VPD-06 | `Process_AppointmentWithoutMedicalRecord_MapMedicalRecordAsNull` | P6 | One appointment with `MedicalRecord = null` → response item `MedicalRecord == null` |
| TC-VPD-07 | `Process_AppointmentWithMedicalRecord_MapMedicalRecordFieldsCorrectly` | P7 | One appointment with `MedicalRecord` populated → response item `MedicalRecord.Id`, `MedicalRecord.RecordType`, `MedicalRecord.ChiefComplaint`, `MedicalRecord.DiagnosisMain` (= Summary), `MedicalRecord.TreatmentPlan` (= Notes), `MedicalRecord.Notes`, `MedicalRecord.IsLocked`, `MedicalRecord.CreatedAt`, `MedicalRecord.Prescriptions` = empty list |
| TC-VPD-08 | `Process_PatientWithoutUser_MapAvatarUrlAsNull` | P8 | Patient with `User = null` → response `AvatarUrl == null` (null-conditional branch) |
| TC-VPD-09 | `Process_PatientWithUserAndAvatarUrl_MapAvatarUrlCorrectly` | P9 | Patient with `User.AvatarUrl = "https://example.com/p.jpg"` → response `AvatarUrl == "https://example.com/p.jpg"` |
| TC-VPD-10 | `Process_AppointmentWithoutService_MapServiceNameAsNull` | P10 | Appointment with `Service = null` → response item `ServiceName == null` |
| TC-VPD-11 | `Process_AppointmentStatus_IsSerializedAsString` | P11 | Appointment with `Status = AppointmentStatus.COMPLETED` → response item `Status == "COMPLETED"` |
| TC-VPD-12 | `Process_AppointmentsOrderedByDescendingDate_ReturnsInSortedOrder` | P5 (multiple) | Two appointments seeded out-of-order (e.g., 2026-02-01 then 2026-01-15) → response items returned in descending date order. Covers `OrderByDescending(a => a.AppointmentDate)` chain |
| TC-VPD-13 | `Process_HappyPath_ResponseShapeHasNoMeta` | P4 | Response `Meta == null` (verifies `CreateSuccessResponse` uses 2-arg `Success(codeMessage, data)` overload) |
| TC-VPD-14 | `Process_HappyPath_AllThreeReposInvokedExactlyOnce` | P4 | Asserts `_doctorRepo.FindByCondition(...)` called once; `_patientRepo.FindByCondition(...)` called once; `_appointmentRepo.FindByCondition(...)` called exactly twice (once for relation check, once for history) |

**Test arrangement**

```
ECS.Test/
└── Services/
    └── DoctorAppointmentPatientManagementServices/      ← existing test folder
        └── ViewPatientDetailServices/                    ← new subfolder
            └── ViewPatientDetailServiceTests.cs          ← new file (14 tests)
```

Plus the MockData helper in:
```
ECS.Test/
└── MockData/
    └── ViewPatientDetailMockData.cs                       ← new file
```

---

## Cleanup

No stray `implementation_plan*.md` exists in the project root. ✅

---

## Test plan summary

- **Total tests**: 14
- **Frameworks**: xUnit + Moq + FluentAssertions + MockQueryable.Moq (all already installed).
- **Coverage tools**: `coverlet.collector 6.0.4` (installed) + `dotnet-reportgenerator-globaltool 5.5.10` (installed).
- **Execution**: `ECS.Test\run-tests.bat` — produces `TestResults\coverage-report\index.html`.

---

## Feedback request

`request_feedback = true` — awaiting user approval to proceed to implementation.